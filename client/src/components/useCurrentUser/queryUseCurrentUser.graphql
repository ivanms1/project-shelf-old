query UseCurrentUser {
  currentUser: getCurrentUser {
    id
    name
    lastName
    email
    role
    projects {
      id
      title
      preview
      description
      siteLink
      repoLink
      isApproved
      createdAt
    }
  }
}
