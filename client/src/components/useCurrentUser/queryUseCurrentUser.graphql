query UseCurrentUser {
  currentUser: getCurrentUser {
    name
    lastName
    email
    role
    projects {
      id
      title
      preview
      description
      siteLink
      repoLink
      isApproved
      createdAt
    }
  }
}
