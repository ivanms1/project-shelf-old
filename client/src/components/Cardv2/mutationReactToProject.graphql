mutation ReactToProject($input: ReactToProjectInput) {
  reactToProject(input: $input) {
    id
    title
    preview
    repoLink
    siteLink
    likes {
      id
      name
      lastName
    }
    description
    isApproved
    createdAt
  }
}
