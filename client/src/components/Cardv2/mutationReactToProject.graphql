mutation ReactToProject($input: ReactToProjectInput) {
  reactToProject(input: $input) {
    id
    title
    preview
    description
    siteLink
    repoLink
    isApproved
    likes {
      id
    }
    favorites {
      id
    }
    createdAt
  }
}
