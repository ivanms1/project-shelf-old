mutation FavoriteProjectMutation($input: FavoriteProjectInput) {
  favoriteProject(input: $input) {
    id
    title
    preview
    description
    siteLink
    repoLink
    isApproved
    likes {
      id
    }
    favorites {
      id
    }
    createdAt
  }
}
