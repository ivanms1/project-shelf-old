mutation CreateUserProject($input: CreateProjectInput) {
  createProject(input: $input) {
    id
    title
    preview
    description
    siteLink
    repoLink
    isApproved
    likes {
      id
    }
    createdAt
  }
}
