query GetAllProjects {
  projects: getProjects(onlyApproved: false) {
    nextCursor
    results {
      id
      title
      author {
        name
        lastName
        email
      }
      preview
      repoLink
      siteLink
      createdAt
      description
      isApproved
    }
  }
}
