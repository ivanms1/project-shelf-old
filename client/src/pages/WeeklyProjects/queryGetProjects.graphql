query GetAllProjects {
  projects: getProjects {
    id
    title
    createdAt
    author {
      name
      lastName
      email
    }
    likes {
      id
    }
    preview
    repoLink
    siteLink
    description
    isApproved
  }
}
