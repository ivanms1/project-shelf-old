query GetAllProjects {
  projects: getProjects {
    id
    title
    createdAt
    author {
      name
      lastName
      email
    }
    preview
    repoLink
    siteLink
    description
    isApproved
  }
}
