query GetAllProjects {
  projects: getProjects {
    id
    title
    author {
      name
      lastName
      email
    }
    preview
    repoLink
    siteLink
    createdAt
    description
    isApproved
  }
}
