#import "../../../graphql/projectFragment.graphql"

query GetAllDissaprovedPojects($cursor: String = null) {
  projects: adminGetNotApprovedProjects(cursor: $cursor) {
    ...ProjectsResponseFragment
  }
}
