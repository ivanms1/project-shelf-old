#import "../../../graphql/projectFragment.graphql"

query GetAllApprovedProjects($cursor: String = null) {
  projects: getProjects(cursor: $cursor, onlyApproved: true) {
    ...ProjectsResponseFragment
  }
}
