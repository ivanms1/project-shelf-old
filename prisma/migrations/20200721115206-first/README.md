# Migration `20200721115206-first`

This migration has been generated by ivanms1 at 7/21/2020, 11:52:06 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Role" AS ENUM ('USER', 'ADMIN');

CREATE TABLE "public"."User" (
"discord" text   ,"email" text  NOT NULL ,"github" text   ,"id" text  NOT NULL ,"lastName" text  NOT NULL ,"name" text  NOT NULL ,"password" text  NOT NULL ,"role" "Role" NOT NULL DEFAULT E'USER',
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Project" (
"authorId" text  NOT NULL ,"description" text  NOT NULL ,"id" text  NOT NULL ,"isApproved" boolean  NOT NULL DEFAULT false,"preview" text  NOT NULL ,"repoLink" text  NOT NULL ,"siteLink" text  NOT NULL ,"title" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_ProjectstLiked" (
"A" text  NOT NULL ,"B" text  NOT NULL )

CREATE UNIQUE INDEX "_ProjectstLiked_AB_unique" ON "public"."_ProjectstLiked"("A","B")

CREATE  INDEX "_ProjectstLiked_B_index" ON "public"."_ProjectstLiked"("B")

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_ProjectstLiked" ADD FOREIGN KEY ("A")REFERENCES "public"."Project"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_ProjectstLiked" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200721115206-first
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,39 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
+enum Role {
+  USER
+  ADMIN
+}
+
+model User {
+  id            String    @id @default(cuid())
+  name          String
+  lastName      String
+  password      String
+  email         String
+  role          Role      @default(USER)
+  github        String?
+  discord       String?
+  projects      Project[]
+  projectsLiked Project[] @relation("ProjectstLiked")
+}
+
+model Project {
+  id          String  @id @default(cuid())
+  title       String
+  preview     String
+  repoLink    String
+  siteLink    String
+  author      User    @relation(fields: [authorId], references: [id])
+  authorId    String
+  description String
+  likes       User[]  @relation(name: "ProjectstLiked")
+  isApproved  Boolean @default(false)
+}
```


